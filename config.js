const config = {
    // For local testing, use the local backend URL.
    // Update this to your Cloud Run URL when deployed.
    backendUrl: 'http://localhost:3000'
  };
  
  export default config;
  